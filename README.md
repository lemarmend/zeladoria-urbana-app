# 🏙️ Zeladoria Urbana - App de Gestão Pública

Sistema para mapeamento colaborativo de problemas urbanos (buracos, iluminação, limpeza), com gestão de status pela prefeitura.

## 🚀 Tecnologias
- **Backend:** Python (FastAPI)
- **Banco de Dados:** SQLite (SQLAlchemy)
- **Frontend:** HTML5 + JS + Leaflet Maps (OpenStreetMap)

## ⚙️ Como rodar

1. Clone o repositório.
2. Crie um ambiente virtual: `python -m venv venv`
3. Ative o venv e instale as dependências: `pip install -r requirements.txt`
4. Rode o servidor: `uvicorn main:app --reload`
5. Abra o endereço `127.0.0.1:8000` no navegador.